const usuario = "luiz";
const senha = "0204";

if (usuario === "luiz" && senha === "0204") {
    console.log("Bem vindo!");
} else if (usuario !== "luiz") {
    console.log("Erro: Usuário inválido.");
} else if (senha !== "0204") {
    console.log("Erro: Senha inválida.");
}
